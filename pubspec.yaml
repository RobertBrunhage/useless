name: useless
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  integration_test:
    sdk: flutter
  logging: ^1.2.0
  provider: ^6.0.5

dev_dependencies:
  all_lint_rules_community: ^0.0.39
  flutter_test:
    sdk: flutter

  intl: ^0.18.0
  intl_utils: ^2.6.1

flutter:
  uses-material-design: true

  fonts:
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter-VariableFont.ttf

flutter_intl:
  enabled: true
